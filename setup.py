from setuptools import setup,find_packages

setup(
  name = 'detect-duplicate',
  packages = find_packages(),
  version = '1.1.1',
  description = 'Detect duplicate elements in a list',
  author = 'Siddharh Jain',
  author_email = 'sidj242@gmail.com',
  url = 'https://github.com/sidj242/detect-duplicate/', # use the URL to the github repo
  keywords = ['detect duplicate','find duplicate', 'list'], # arbitrary keywords
  classifiers = [],
  install_requires = [],
)
